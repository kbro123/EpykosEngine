# M2 gate measurement round — fingerprint `d448afd70180`

Intel Xeon W-3223, 8 physical / 16 logical cores, Apple clang 21.0.0 (clang-2100.1.1.101), flags
`-O3 -march=x86-64-v3 -fno-math-errno`, release preset, engine commit `8914934` (integrate/m1-m5 tip at the M2 gate,
fresh worktree, all tests green under release and reference). Every row was written by `bench/run.sh` (20 Google
Benchmark repetitions x >= 0.2 s; min / median / p90 over the 20 repetition means of real time, us; D29) with the
machine quiet: no compiler or test running, **1-minute load 5.74 before the first run and 3.08 after the last**
(cores/2 = 8 rule met on every file; the loads per file are inside the files). The three runs were sequential:
`hand_m1_hand` (5.74 -> 4.48), `exec_m1_interp` (4.48 -> 3.24), the adjoint run (3.24 -> 3.08; written as `m2.json`
with `--name m2`, re-keyed to `adjoint_m1_adjoint.json` by M2/m2-fix, D34). The `DIRTY` flag on the second
and third files is the first file having overwritten the tracked `hand_m1_hand.json`; no source differed from
`8914934`.

## The M1 benches re-measured at the M2 gate (self-regression check, D29)

`hand_m1_hand.json` and `exec_m1_interp.json` now hold these runs (they replace the P6 attempt-5 rows Q5 migrated;
the M1 evidence stays in `m1.json`, `README.md` and the `m1_p6_*` files). `scripts/perf_gate.py exec_m1_interp.json
hand_m1_hand.json` against `baseline.json` (still the M1 result at `b32182a`): **PASS**, 47 rows compared, every
ratio within 0.997-1.029, no regression, nothing faster than 0.8x. The D27 targets on the fresh pair:

| target | fresh numerator | fresh denominator | ratio | limit |
|---|---:|---:|---:|---|
| m1_b1 (B=1, std::exp) | BM_InterpEval tile 512: 56.73 | BM_HandEval/2: 54.38 | **1.043** | <= 1.3: PASS |
| m1_b64 (B=64, std::exp) | BM_InterpEvalBatch tile 512 / lane_tile 32: 1585.66 | BM_HandEvalBatch/2: 1487.25 | **1.066** | <= 1.1: PASS |

(M1 at `b32182a`: 1.044 / 1.079. The hand kernel's batched rows are 1.3-1.6 % slower than in the M1 round, the
interpreter's within 0.5 %; both well inside the 1.25x rule.) The baseline was **not** moved for these runs.

## `adjoint_m1_adjoint.json` (was `m2.json`): value + adjoint on the M1 book (informational, D9; never an M2 gate)

`adjoint_m1_adjoint_bench` filtered to `BM_AdjointEval` (B=1, the D15 tiles), `BM_AdjointEvalBatch` at lane tile 8
(the Q3-best tile 128 and the default 256) and `BM_AdjointBuild`. One `Adjoint::run` = the 1,001 outputs plus
d(book pv)/dz (`out_bar = e_book`) per lane; state written fresh each repetition, tables in the constructor.

| row | config | min | median | p90 |
|---|---|---:|---:|---:|
| value + adjoint, B=1 | tile 128 | 411.2 | 416.4 | 419.2 |
| value + adjoint, B=1 | tile 256 | 412.0 | **416.2** | 418.7 |
| value + adjoint, B=1 | tile 512 | 415.5 | 419.6 | 422.2 |
| value + adjoint, B=64 | tile 128, lane_tile 8 (228.3 us/state) | 14415.6 | **14609.6** | 14750.5 |
| value + adjoint, B=64 | tile 256, lane_tile 8 | 14955.1 | 15141.1 | 15288.1 |
| AdjointBuild (constructor) | | 2472.5 | 2743.0 | 2935.6 |

**Adjoint cost ratio at B=1 (value + adjoint over value only, interpreter, same round): 416.24 / 56.73 = 7.34**
(best tile of each: adjoint at 256, interpreter at 512; at the same tile 256 it is 416.24 / 58.37 = 7.13; against the
hand kernel variant 2 it is 7.65). At B=64: 14609.6 / 1585.7 = 9.21 (228.3 vs 24.8 us per state). The Q3 numbers
(`m2_adjoint.md`, load-tainted at 13.7 -> 10.2) were 422.5 us and 16172.9 us: on the quiet machine the B=1 row is
1.5 % faster and the B=64 row 9.7 % faster. Where the time goes is as Q3 described (D31): every row value
materialised (42,314 rows, none of the interpreter's fusions in the reverse), intermediate steps recomputed, pulls
through 56,937 gather and 37,866 segment reader entries; the reverse of a fused group is M3's catalogue / rewrite
work. `baseline.json` carries these six rows under the run name `adjoint_m1_adjoint`, the name `bench/run.sh`
derives from the binary, so a plain `bench/run.sh build/release/bench/adjoint_m1_adjoint_bench` followed by
`scripts/perf_gate.py adjoint_m1_adjoint` is gated at 1.25x on these six rows; a run without the filter above
reports its other rows as new, not gated. (The round seeded them as run `m2`, the ad hoc `--name` of this file,
which gated only a run made with `--name m2` and left the default invocation refused for want of a baseline;
M2/m2-fix re-keyed the file and the entry with the medians unchanged, D34.)
